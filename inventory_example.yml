# ==========================================
# This inventory serves as an example only.
# Replace <IP> with actual IP addresses.
# ==========================================
all:
  hosts:
    node1:
      ansible_host: <IP>
    node2:
      ansible_host: <IP>
    node3:
      ansible_host: <IP>
  children:
    provider:
      hosts:
        node1:
