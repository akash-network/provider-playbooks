# ==========================================
# EXAMPLE INVENTORY - FOR MANUAL EXECUTION
# ==========================================
# This inventory serves as an example only.
# ==========================================
all:
  children:
    local:
      hosts:
        localhost:
          ansible_connection: local

    target:
      hosts:
        <server1>:
          ansible_user: root
