---
- name: Tailscale Playbook
  hosts: all
  vars_files:
    - versions.yml
  become: true
  become_user: root
  roles:
    - tailscale
  tags:
    - tailscale

- name: Preflight Playbook
  hosts: all
  vars_files:
    - versions.yml
  become: true
  roles:
    - preflight
  tags:
    - preflight

- name: k3s Playbook
  hosts: all
  vars_files:
    - versions.yml
  become: true
  become_user: root
  serial: "{{ serial_hosts | default(1) }}"
  pre_tasks:
    - name: Set node role
      ansible.builtin.set_fact:
        is_control_plane: "{{ inventory_hostname in groups['kube_control_plane'] }}"
  roles:
    - k3s
  tags:
    - k3s

- name: OS Playbook
  hosts: k8s_cluster
  vars_files:
    - versions.yml
  become: true
  become_user: root
  roles:
    - os
  tags:
    - os

- name: Local Path Provisioner Playbook
  hosts: node1
  vars_files:
    - versions.yml
  become: true
  roles:
    - local-path
  tags:
    - local-path

- name: Retrieve OP items
  hosts: localhost
  vars_files:
    - versions.yml
  roles:
    - op
  tags:
    - op

- name: GPU Playbook
  hosts: kube_node
  vars_files:
    - versions.yml
  become: true
  become_user: root
  roles:
    - gpu
  tags:
    - gpu

- name: Persistent Storage rook-ceph playbook
  hosts: node1
  vars_files:
    - versions.yml
  become: true
  become_user: root
  serial: "{{ serial_hosts | default(1) }}"
  roles:
    - rook-ceph
  tags:
    - rook-ceph

- name: Provider Playbook
  hosts: node1
  vars_files:
    - versions.yml
  become: true
  become_user: root
  serial: "{{ serial_hosts | default(1) }}"
  roles:
    - provider
  tags:
    - provider
